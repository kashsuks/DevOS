#include "terminal.h"

void window_create(uint8_t width, uint8_t height) {
    terminal_writestring("Creating window...\n");
    // Future implementation for creating a window and rendering inside it
}
